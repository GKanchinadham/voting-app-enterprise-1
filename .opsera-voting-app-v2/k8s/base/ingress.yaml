apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: voting-app-v2
  labels:
    app.kubernetes.io/name: voting-app-v2
    app.kubernetes.io/component: ingress
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - opsera-voting-app-v2-dev.agent.opsera.dev
    secretName: voting-app-v2-dev-tls
  rules:
  # Vote frontend (main entry point)
  - host: opsera-voting-app-v2-dev.agent.opsera.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: vote
            port:
              number: 80
